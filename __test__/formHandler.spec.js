import {validateForm, submitForm} from '/src/client/js/formHandler'

describe(
    "test suite for validateForm",
    ()=>{
            test("test happy path",
            ()=> {
                const event = {
                    target: 
                }
                
            }),
            test("test bad location",
            ()=> {

            }),
            test("test bad departing date",
            ()=> {

            }),
            test("test bad location",
            ()=> {

            }),
            test("test bad departing date",
            ()=> {

            })
    })